<view class="wrap">
  <text class="title">类型</text>
  <view class="types-wrap" bindtap="typesChange">
    <view class="item" wx:for="{{movieTypes}}" wx:key="{{index}}" data-name="{{item}}">{{item}}</view>
  </view>
</view>